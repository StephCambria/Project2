<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>What's in the Fridge?</title>
    <link href="./css/dist/output.css" rel="stylesheet" />
   <link href="./css/dist/style.css" rel="stylesheet" />
    <link rel="stylesheet" href="//code.jquery.com/ui/1.13.2/themes/base/jquery-ui.css">
    <script src="https://code.jquery.com/jquery-3.6.0.js"></script>
    <script src="https://code.jquery.com/ui/1.13.2/jquery-ui.js"></script>

    <script>
      $( function() {
        $( "#sortable" ).sortable({
          revert: true
        });
        $( "#draggable" ).draggable({
          connectToSortable: "#sortable",
          helper: "clone",
          revert: "invalid"
        });
        $( "ul, li" ).disableSelection();
      } );
      </script>
  </head>
  <body class="m-0 p-0">
    <nav class="navbar w-100 h-100 flex align-middle justify-evenly p-5 z-10">
      <ul class="links-container flex list-none">
        <li class="link" id="home-link"><a href="/">Home</li>
        <li class="link" id="fridge-link">
          <a href="/fridge">Check the Fridge!</a>
        </li>
        <li class="link text-teal-500 ml-10 p-10" id="grocery-link">
          <a href="/grocery">Grocery List</a>
        </li>
        <li class="link" id="recipe-link">
          <a href="/dashboard">Cutting Board</a>
        </li>
        <!-- Conditionally render login or logout links -->
        {{#if loggedIn}}
        <li class="link" id="logout-link">Logout</li>
    {{else}}
        <li class="link" id="login-link"><a href="/login">Login</a></li>
        <li class="link" id="signup-link"><a href="/signup">Sign Up</a></li>
      {{/if}}
      </ul>
    </nav>

    <br>
    
      <!-- Render the sub layout -->

        <!-- Render the sub layout -->
<main class="m-30 p-20">
  <div id="listContainer">
    <h3 class="font-semibold mt-10 p-5 text-3xl">Welcome to your grocery list!</h3>
      <br>
      <h3 class="font-semibold mt-10 p-5">Here you can view and sort through everything you need to get.</h3>
    
    <br>
  
    <p class="font-medium mt-10 p-5">Click and drag each item to organize them.</p>
  
    <br>
  
<div class="p-20">
    <h1 style="text-align:center; color:green;">
        Add A Food Item
    </h1>
  
    <!-- Form to add item -->
    <form action="#" style="text-align:center;">
  
        <!-- Type of Element -->
        {{!-- <label for="type">
            Generate List
        </label>
  
        <input 
            type="text" 
            id="type" 
            placeholder="" 
            value="li"
            readonly
            />
        <br /><br /> --}}
  
        <!-- Text/Value for the element --->
        <label for="value">
            Food Item
        </label>
  
        <input type="text" 
            id="value" 
            placeholder="i.e. Apples" 
            value="" />
        <br /><br />
  
        <!-- Submit the Form -->
        <button type="button" 
            onClick="addItem()">
            Add
        </button>
    </form>
</div>

    <br></br>

      <div>
        <ul id="sortable" class="box-border h-32 p-1 m-20">
        <!-- New li elements will be added here -->
        </ul>
      </div>
    
  </main>

  <br>

    <footer>
      
    </footer>


  <!-- Render script for logged in users only -->
{{#if loggedIn}}
  <script src="/js/logout.js"></script>
{{/if}}
<script src="/js/grocery.js"></script>
  </body>

</html>